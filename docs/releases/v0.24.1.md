# v0.24.1 — 关于页重做 + 构建修复

## 关于页

- **左右分栏布局** — 左侧应用信息（沉浸式垂直居中），右侧更新日志（标题固定 + 列表独立滚动）
- **更新日志数据源** — 改为从 `docs/releases/` 读取各版本发布说明（jsDelivr CDN），显示 tag + 完整 markdown 内容，可展开卡片
- **上下渐隐** — 更新日志列表上下边缘内容渐隐（CSS mask），滚动更柔和
- **自动更新 UI** — 检查更新按钮、下载进度条、重启安装按钮
- 底部版权区优化，工作室 logo 淡化

## 修复

- **GitHub API 403** — Rust sidecar reqwest 禁用系统代理（macOS Clash 代理对 api.github.com 返回 403），GitHub API / jsDelivr CDN 直连更可靠
- **检查更新按钮报错** — IPC handler 总是注册，dev 模式返回明确状态
- **打包图裂** — Vite `base: './'` + 组件资源引用改相对路径

## 构建

- node-gyp 升级至 12.x（支持 VS 2026，修复 Windows Server 2025 镜像构建）
- @electron/rebuild worker promisify 兼容补丁（node-gyp 11+ async 命令）
